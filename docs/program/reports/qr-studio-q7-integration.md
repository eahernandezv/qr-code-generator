# QR Studio — Q7 deterministic path integration

Status: **READY_FOR_PRODUCT_ARCHITECT_REVIEW**

- Owner: QR Studio / Studio-Commerce
- Reported UTC: 2026-08-15T12:54:24Z
- Authoritative SOW: `docs/program/handoffs/qr-studio-q7-integration.md`
- SOW SHA-256: `b84fce32966d0d80ceb935847a1cc2a7f1c02cb757021e70ee3c84f20b5f591e`
- Exact baseline: `e641b673d81afed6228346d305bd31f77a513e16`
- Branch: `studio/integrate-q7-deterministic-path`
- Implementation/evidence commit: `33d8b39ec21cffd88e834184da022863aabc6022`
- Push state: blocked by unavailable GitHub credentials on this host; remote branch was absent before the attempt
- PR/merge: not opened; Product Architect retains both authorities

## Outcome

Studio now presents two deterministic customer paths:

1. **Level 1 Safe** links to the existing universal deterministic Studio flow.
2. **Q7 Image-Fit Premium** consumes the frozen Core Image-Fit response and maps customer labels exactly as `Mellow → readable`, `Balanced → balanced`, and `Punchy → image_first`.

The integration does not expose a provider-generative model, provider selector, or provider-facing copy. Punchy remains visibly backed by Core's experimental `image_first` mode and cannot export.

Studio verifies inline Core artifact bytes against the declared SHA-256 before accepting the response. It never upgrades browser state, payment state, or a candidate status into export authority. Download is enabled only when all frozen Core facts agree: scan pass, non-Image-first mode, `export_allowed`, `preview_export_parity: proven`, and an exact Core export artifact. The current accepted Q7 boundary returns parity `not_proven`, unpaid preview, and a reserved short link, so export remains visibly denied. Checkout is not bound on this route and therefore cannot rewrite QR bytes.

When Image-Fit generation fails or does not qualify, previous candidate evidence is removed and the UI explains the failure while offering the deterministic Level 1 Safe path. The Level 1 fallback artifact is reproduced directly from accepted Core code and hash-checked; its frozen authority remains `core_internal_fallback_only`, so this lane does not invent a customer download grant.

## Changed files

Implementation and tests:

- `apps/web/src/components/ImageFitQrConcept.tsx`
- `apps/web/src/lib/imageFitGenerationClient.ts`
- `apps/web/src/lib/imageFitGenerationClient.test.ts`
- `apps/web/src/App.test.tsx`
- `apps/web/e2e/level2-image-fit-spike.spec.ts`
- `apps/web/scripts/generate-q7-studio-evidence.mjs`

Evidence:

- `docs/program/evidence/studio-q7-integration/integration-evidence.json`
- `docs/program/evidence/studio-q7-integration/sha256.txt`
- `docs/program/evidence/studio-q7-integration/artifacts/{readable,balanced,image-first,fallback-level1}.svg`
- `docs/program/evidence/studio-q7-integration/browser/{mobile-ready-to-generate,mobile-creator-response-bound,mobile-creator-unavailable-fail-closed}.png`
- `docs/program/evidence/studio-q7-integration/browser/browser-proof.json`
- `docs/program/reports/qr-studio-q7-integration.md`

No frozen contract, Core implementation, commerce authority, payment, short-link resolver, or provider integration file changed.

## Reproducible evidence

`integration-evidence.json` was generated by running the accepted Q7 optimizer from the exact baseline against the frozen bold-diamond target. It records each supported mode's preview hash and truthful export state:

| Studio mode | Core mode | Score version | Preview SHA-256 | Checkout/final hash | State |
|---|---|---|---|---|---|
| Mellow | readable | `image-fit-scan-first-appearance-q7` | `ba9517638245d6f0b7c478c8b758a87c669c8f9accc840476ee87fe54628ecd2` | `null` / `null` | denied by parity/payment/short-link gates |
| Balanced | balanced | `image-fit-scan-first-appearance-q7` | `2e1aebf8e03466b5391f5b45a28d4259901ed964cc1cf3d9c5953c8fb822165c` | `null` / `null` | denied by parity/payment/short-link gates |
| Punchy | image_first | `image-fit-scan-first-appearance-q7` | `350bd24a526b474eddb545bface00b709098f7fbbc39e4e4c29404c22be38c58` | `null` / `null` | additionally experimental/export-blocked |
| Level 1 Safe fallback | deterministic fallback | n/a | `f453ef42520daa427212ce61b4b697f2ba6177652ea7004aa1734a4a3f8a2e7b` | `null` / `null` | Core internal fallback only |

Null checkout/final hashes are deliberate fail-closed evidence: no authorized final export exists in the frozen Q7 preview response, so Studio creates no parallel bytes and makes no parity claim.

Browser proof at 390×844 recorded no horizontal overflow, no page errors, `exportPayloadBound=false`, `checkoutBound=false`, candidate removal after failure, and the exact request controls sent to Core. Final screenshot inspection confirmed visible Level 1/Q7 navigation, Mellow/Balanced/Punchy controls, the Core-gate denial, and no provider model surface. No clipping or material visual defect was observed.

## Commands and observed results

Install and dependency builds:

```text
CI=1 npm exec --yes pnpm@9.0.0 -- install --frozen-lockfile
# all 8 workspace projects; lockfile current; exit 0
npm exec --yes pnpm@9.0.0 -- --filter @qr/qr-core build
# exit 0
npm exec --yes pnpm@9.0.0 -- --filter @qr/artistic-qr build
# exit 0
```

Focused integration tests:

```text
npm exec --yes pnpm@9.0.0 -- --filter @qr/web exec vitest run src/lib/imageFitGenerationClient.test.ts src/App.test.tsx
# 2 files passed; 27 tests passed
```

Touched-package gates:

```text
npm exec --yes pnpm@9.0.0 -- --filter @qr/web test
# 15 files passed; 137 tests passed
npm exec --yes pnpm@9.0.0 -- --filter @qr/web lint
# exit 0; zero warnings
npm exec --yes pnpm@9.0.0 -- --filter @qr/web build
# tsc + Vite; 134 modules transformed; exit 0
```

Evidence generation and integrity:

```text
node apps/web/scripts/generate-q7-studio-evidence.mjs
(cd docs/program/evidence/studio-q7-integration && sha256sum -c sha256.txt)
# 4 SVG artifacts, integration JSON, 3 browser screenshots, and browser JSON: all OK
```

Focused browser gate:

```text
PATH=<Node-20-bin>:<pnpm-9-bin>:$PATH \
FONTCONFIG_FILE=$HOME/.cache/ms-playwright/local-fonts.conf \
LD_LIBRARY_PATH=$HOME/.cache/ms-playwright/local-libs/root/usr/lib/x86_64-linux-gnu \
node node_modules/@playwright/test/cli.js test --grep "mobile request binding"
# 1 passed (4.9s); port 4173 closed after run
```

The canonical `pnpm test:e2e` attempt was blocked before test execution because host Node is 18.19.1 while Playwright requires Node 20+. The final command uses the same repository Playwright config, web server, browser, font setup, grep, and test file under an npm-provided Node 20.20.2 runtime. An initial corrected-Node attempt also exposed the host's actual local-library path (`local-libs/root/...`); the final run used that path and passed.

Final hygiene before report creation:

```text
git diff --check
# exit 0
ss -ltnp '( sport = :4173 )'
# no listener
```

Push attempt after the implementation/evidence commit:

```text
git ls-remote --heads origin studio/integrate-q7-deterministic-path
# absent
git push -u origin studio/integrate-q7-deterministic-path
# failed before remote mutation: configured credential helper points to missing /home/hermes/.local/bin/gh;
# HTTPS could not read a username, and SSH authentication also returned Permission denied (publickey)
```

## Failure and no-side-effect coverage

- Inline artifact hash mismatch rejects the complete response.
- Cross-request and malformed responses remain rejected.
- Network failure removes candidate evidence and offers Level 1 Safe.
- Input edits invalidate selected and board artifacts.
- Scan failure, missing parity, unpaid preview, uncommitted short link, missing export artifact, and Image-first experimental state all keep download disabled.
- Browser evidence confirms no checkout or export payload was bound on failure.
- Existing web commerce/export tests passed as part of the 137-test package suite; this integration does not alter checkout or export bytes.

## Remaining risks and Product Architect checks

1. The frozen Q7 HTTP response exposes fallback availability/reason but not a customer-authorized fallback artifact. Studio therefore routes customers to the existing Level 1 Safe flow and does not fabricate fallback authority.
2. Current Q7 candidates remain export-blocked because preview/export parity is `not_proven`, the preview is unpaid, and the optimized short link is only reserved. Positive Q7 export cannot be claimed until Core/commerce/short-link authorities return an exact authorized artifact.
3. Automated scan evidence remains the Core-owned controlled decoder scope; no physical-device or print scan was performed here.
4. Product Architect should independently reproduce the evidence manifest, run the final branch gates, and decide PR/release acceptance.
