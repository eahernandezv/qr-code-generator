import { createHash } from 'node:crypto'
import { mkdirSync, readFileSync, writeFileSync } from 'node:fs'
import { resolve } from 'node:path'
import { createRequire } from 'node:module'
const requireFromArtistic = createRequire(new URL('../../../packages/artistic-qr/package.json', import.meta.url))
const { PNG } = requireFromArtistic('pngjs')

const base = process.env.STUDIO_URL ?? 'https://placeholder-online.com'
const apiPrefix = process.env.ARTISTIC_QR_API_PREFIX ?? (base.includes('127.0.0.1') || base.includes('localhost') ? '' : '/api/artistic-qr')
const outDir = resolve(process.cwd(), '../../docs/program/evidence/design-style-options-clean-first')
mkdirSync(outDir, { recursive: true })

function sha256(buffer) { return createHash('sha256').update(buffer).digest('hex') }
function dataUrl(buffer) { return `data:image/png;base64,${buffer.toString('base64')}` }
function set(px, x, y, r, g, b, a = 255) { if (x < 0 || y < 0 || x >= px.width || y >= px.height) return; const o = (y * px.width + x) * 4; px.data[o]=r; px.data[o+1]=g; px.data[o+2]=b; px.data[o+3]=a }
function ellipse(px, cx, cy, rx, ry, color) { for (let y = Math.floor(cy - ry); y <= Math.ceil(cy + ry); y++) for (let x = Math.floor(cx - rx); x <= Math.ceil(cx + rx); x++) if (((x - cx) ** 2) / (rx ** 2) + ((y - cy) ** 2) / (ry ** 2) <= 1) set(px, x, y, ...color) }
function triangle(px, points, color) { const [a,b,c]=points; const minX=Math.floor(Math.min(a[0],b[0],c[0])), maxX=Math.ceil(Math.max(a[0],b[0],c[0])); const minY=Math.floor(Math.min(a[1],b[1],c[1])), maxY=Math.ceil(Math.max(a[1],b[1],c[1])); const area=(p1,p2,p3)=>(p1[0]*(p2[1]-p3[1])+p2[0]*(p3[1]-p1[1])+p3[0]*(p1[1]-p2[1]))/2; const A=Math.abs(area(a,b,c)); for(let y=minY;y<=maxY;y++) for(let x=minX;x<=maxX;x++){ const p=[x,y]; const A1=Math.abs(area(p,b,c)),A2=Math.abs(area(a,p,c)),A3=Math.abs(area(a,b,p)); if(Math.abs(A-(A1+A2+A3))<0.5) set(px,x,y,...color)} }
function makeIcon(kind) { const png = new PNG({ width: 180, height: 132, colorType: 6 }); for (let i=0;i<png.data.length;i+=4) { png.data[i]=250; png.data[i+1]=250; png.data[i+2]=247; png.data[i+3]=255 } if (kind === 'fox') { triangle(png, [[58,42],[72,10],[84,47]], [212,92,28]); triangle(png, [[122,42],[108,10],[96,47]], [212,92,28]); ellipse(png,90,69,54,46,[219,96,32]); ellipse(png,70,82,22,19,[255,247,235]); ellipse(png,110,82,22,19,[255,247,235]); ellipse(png,90,86,9,6,[31,41,55]); ellipse(png,73,62,5,7,[17,24,39]); ellipse(png,107,62,5,7,[17,24,39]) } else { triangle(png, [[58,45],[66,10],[82,48]], [86,97,115]); triangle(png, [[122,45],[114,10],[98,48]], [86,97,115]); ellipse(png,90,70,55,45,[107,114,128]); ellipse(png,90,86,28,18,[226,232,240]); ellipse(png,90,88,9,6,[15,23,42]); ellipse(png,73,62,5,6,[17,24,39]); ellipse(png,107,62,5,6,[17,24,39]) } return PNG.sync.write(png) }
async function post(path, json) { const res = await fetch(new URL(`${apiPrefix}${path}`, base), { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify(json) }); const body = await res.json().catch(() => undefined); if (!res.ok) throw new Error(`${path} ${res.status} ${JSON.stringify(body)}`); return body }
function targetFromAsset(asset, complexity) { return { image_ref: asset.uri, mime_type: asset.mimeType, width_px: asset.width, height_px: asset.height, sha256: asset.sha256, complexity } }
async function uploadAndPrepare(kind) { const source = makeIcon(kind); writeFileSync(resolve(outDir, `${kind}-source.png`), source); const upload = await post('/image-fit/uploads', { data_url: dataUrl(source) }); const readiness = await post('/image-readiness/assess', { requestId: `style-${kind}-${Date.now()}`, sourceAsset: upload.source_asset, intendedUse: 'level2-image-fit', payloadPreview: `https://example.com/${kind}`, constraints: { preserveImageColors: true, preserveSubjectCentering: true, allowCrop: true, allowUpscale: true, maxPreparedDimension: 1024 } }); const prepared = readiness.report.preparedAsset ? targetFromAsset(readiness.report.preparedAsset, upload.target_image.complexity) : upload.target_image; return { sourceSha256: sha256(source), upload, readiness: readiness.report, prepared } }
async function generate(kind, prepared, style) { const generation = await post('/image-fit/candidates', { request_id: `l2req-${style}-${kind}-${Date.now()}`, destination: { kind: 'url', normalized_url: `https://example.com/${kind}`, display_url: `https://example.com/${kind}`, safety: { verdict: 'pass', policy_version: 'design-style-proof' } }, target_image: prepared, user_controls: { treatment: 'pixel_blend', strength: 'balanced', detail: 'detailed', image_embedding_style: style, link_mode: 'optimized_short_link' }, constraints: { max_candidates: 12, max_search_ms: 45000, allowed_ecc: ['Q','H'], allowed_masks: [0,1,2,3,4,5,6,7], allowed_versions: [8,9,10,11,12] }, entitlement_context: { mode: 'preview', export_entitled: false } }); const candidates = generation.result.candidates.filter(c => c.status === 'validated' && c.scan_evidence.verdict === 'pass'); const saved = []; for (const c of candidates) { const a = c.artifacts[0]; if (a?.uri?.startsWith('data:image/png;base64,')) { const bytes = Buffer.from(a.uri.replace(/^data:image\/png;base64,/, ''), 'base64'); const file = `${kind}-${style}-${c.image_treatment.logo_size ?? c.mode}.png`; writeFileSync(resolve(outDir, file), bytes); saved.push({ size: c.image_treatment.logo_size, file, sha256: a.sha256, candidate_id: c.candidate_id, score_version: c.image_fit_evidence.score_version, checks: `${c.scan_evidence.checks_passed}/${c.scan_evidence.checks_total}`, recognition_score: c.image_fit_evidence.recognition_score, blockers: c.export_authority.blockers }) } } return { candidateCount: generation.result.candidates.length, validatedCount: candidates.length, saved } }
function text(sheet, label, x, y) { const bits = ch => ch.charCodeAt(0).toString(2).padStart(8,'0'); let cx=x; for (const ch of label.toUpperCase().slice(0,24)) { const b=bits(ch); for (let bit=0; bit<8; bit++) if (b[bit] === '1') rect(sheet,cx+bit*2,y,1,9,15,23,42); cx += 20 } }
function rect(px,x,y,w,h,r,g,b,a=255){ for(let yy=y; yy<y+h; yy++) for(let xx=x; xx<x+w; xx++) set(px,xx,yy,r,g,b,a) }
function blit(sheet, src, dx, dy, dw, dh) { for(let y=0;y<dh;y++) for(let x=0;x<dw;x++){ const sx=Math.floor(x*src.width/dw), sy=Math.floor(y*src.height/dh), so=(sy*src.width+sx)*4; set(sheet, dx+x, dy+y, src.data[so], src.data[so+1], src.data[so+2], src.data[so+3]) } }
function contactSheet(cells) { const cellW=290,imgBox=238,top=42,gap=18,cols=4,rows=Math.ceil(cells.length/cols); const sheet=new PNG({width:cols*cellW,height:rows*(top+imgBox+gap),colorType:6}); for(let i=0;i<sheet.data.length;i+=4){sheet.data[i]=248;sheet.data[i+1]=250;sheet.data[i+2]=252;sheet.data[i+3]=255} for(let i=0;i<cells.length;i++){ const col=i%cols,row=Math.floor(i/cols),x=col*cellW,y=row*(top+imgBox+gap); rect(sheet,x+10,y+10,cellW-20,top+imgBox-2,255,255,255); text(sheet,cells[i].label,x+20,y+22); const img=PNG.sync.read(cells[i].bytes); const scale=Math.min(imgBox/img.width,imgBox/img.height); const dw=Math.round(img.width*scale),dh=Math.round(img.height*scale); blit(sheet,img,x+Math.floor((cellW-dw)/2),y+top+Math.floor((imgBox-dh)/2),dw,dh) } const out=resolve(outDir,'clean-logo-vs-art-blend-contact-sheet.png'); writeFileSync(out, PNG.sync.write(sheet)); return out }

const results=[]; const cells=[]
for (const kind of ['fox','wolf']) { const prep=await uploadAndPrepare(kind); cells.push({ label: `${kind} source`, bytes: makeIcon(kind) }); for (const style of ['clean_logo_fit','embedded_image_fit']) { const gen=await generate(kind, prep.prepared, style); for (const pick of gen.saved.filter(s => ['small','medium','large'].includes(s.size))) cells.push({ label: `${kind} ${style === 'clean_logo_fit' ? 'Clean' : 'Blend'} ${pick.size}`, bytes: Buffer.from(readFileSync(resolve(outDir, pick.file))) }); results.push({ kind, style, ...prep, generation: gen }) } }
const sheet = contactSheet(cells)
const proof = { schema_version: 'qr-design-style-options-clean-first.v1', base, createdAt: new Date().toISOString(), sheet, results }
writeFileSync(resolve(outDir, 'proof.json'), JSON.stringify(proof, null, 2))
console.log(JSON.stringify({ outDir, sheet, results: results.map(r => ({ kind: r.kind, style: r.style, decision: r.readiness.decision, proofPass: r.readiness.proof.pass, validatedCount: r.generation.validatedCount, sizes: r.generation.saved.map(s => ({ size: s.size, checks: s.checks, score_version: s.score_version, sha256: s.sha256 })) })) }, null, 2))
