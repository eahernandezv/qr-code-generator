{
  "name": "qr-code-generator",
  "version": "0.1.0",
  "private": true,
  "description": "Artistic QR Code Generator - Flagship MVP",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "vitest run tests/e2e",
    "test:contracts": "vitest run packages/contracts",
    "lint": "echo 'Lint script not yet configured'"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
